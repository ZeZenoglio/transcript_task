"""FastAPI service (Phase 7): jobs, config, models, and benchmark endpoints
around the same pipeline stages the CLI uses. See `app.py` for the actual
FastAPI app and README.md's "API" section for usage.
"""

from __future__ import annotations
